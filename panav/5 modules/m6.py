from init import append1, extend1, pop1, remove1
from init import slen2, adds2, remove2
from init import len3, add3, keys3, values3

lst = [1, 2, 3]
print(append1(lst, 4))

s = {10, 20, 30}
print(adds2(s, 40))

d = {"name": "Rigel"}
print(add3(d, "age", 25))